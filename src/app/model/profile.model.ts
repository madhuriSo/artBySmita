export interface Profile {
    _id: string;
    name: string;
    imagePath: string;
  }

export interface ProdImage {
    _id: string;
    productId:string;
    name: string;
    imagePath: string;
  }

   